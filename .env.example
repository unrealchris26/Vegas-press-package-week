# Copy to .env.local and fill in. Never commit the real value.
# GoHighLevel inbound webhook trigger URL:
#   Automation > Workflows > your workflow > Inbound Webhook trigger > copy URL
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/<locationId>/webhook-trigger/<uuid>
